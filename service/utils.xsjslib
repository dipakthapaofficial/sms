/**
 * This function is used to check whether the given element is empty, null or undefined
 * @param element
 * @returns
 */
function checkNotEmptyAndNotNull(element){
	return element != null && element != '' ? true : false;
}